query GetViewerInfo {
    viewer {
        watchingCount
        recordsCount
        followersCount
        followingsCount
        description
        annictId
        username
        name
        avatarUrl
    }
}
