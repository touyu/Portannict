query GetViewerInfo {
    viewer {
        watchingCount
        recordsCount
        followersCount
        followingsCount
        description
        username
        name
        avatarUrl
    }
}
