query GetViewerWorks($state: StatusState) {
    viewer {
        works(state:$state, first:10) {
            edges {
                node {
                    title
                    image {
                        recommendedImageUrl
                        twitterAvatarUrl
                    }
                }
            }
        }
    }
}
