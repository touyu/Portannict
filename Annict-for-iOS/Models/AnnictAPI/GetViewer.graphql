query GetViewer {
    viewer {
        ... UserDetails
        backgroundImageUrl
        recordsCount
        description
        followingsCount
        followersCount
    }
}

fragment UserDetails on User {
    avatarUrl
    name
    username
}
