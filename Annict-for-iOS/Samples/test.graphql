query GetViewer {
    viewer {
        ... UserDetails
        annictId
        backgroundImageUrl
        recordsCount
        description
        followingsCount
        followersCount
    }
}

fragment UserDetails on User {
    avatarUrl
    name
    username
}
